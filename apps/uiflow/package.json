{
  "name": "uiflow",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=development nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "nx": {
    "targets": {
      "build": {
        "outputs": [
          "{projectRoot}/.output",
          "{projectRoot}/.nuxt"
        ]
      }
    },
    "tags": [
      "scope:uiflow"
    ]
  }
}
